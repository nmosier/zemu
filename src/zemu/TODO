zvar_localaddr -- now returns file offset, not p-addr
zvar_fetch, zvar_store -- needs to be fixed


;;;; converting window from appvar to file ;;;;
[X] zwin.bufptr to offset
[X] zwin.bufanc to offset
